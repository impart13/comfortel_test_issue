# Ответы на вопросы:

## Задание 1: API и Архитектура (Анализ логов)
1. Произошло двойное (повторное) списание средств. Создано 2 транзации (5501 и 5502), и по каждой транзакции было списано 100 со счета пользователя. В итоге пользователь заплатил 200 вместо 100.
2. Идемпотентность. Когда вызов одной и той же операции с одинаковыми входными данными равнозначен однокрантому ее вызову.
3. Уникальный ключ для каждой транзакции.

## Задание 2: Траблшутинг (Root Cause Analysis)
1. S3 клиент не может выполнить операцию PutObject из-за отсуствия прав доступа (код ошибки Access Denied)
2. Баг назаначу на DevOps для настройки конфигурации доступа.

## Задание 3: Тест-дизайн (Security & Logic)
1. Попытка изменить чужой профиль через тело запроса:
  {
    "user_id": 101,
    "email": "test@test.com",
    "role": "user"
  }

3. Попытка изменить роль через тело запроса:
  {
    "user_id": 100,
    "email": "test@test.com",
    "role": "admin"
  }
